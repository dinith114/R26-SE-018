/* Copy this to secret.js and fill in the real key.
   secret.js is gitignored: the repository is public, so a key committed here is
   a key published to the world. */
export const API_KEY = 'paste-the-key-from-the-server-here';
