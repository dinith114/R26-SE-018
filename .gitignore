# ===========================
# Python
# ===========================
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.Python
env/
venv/
venv*/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# ===========================
# Jupyter Notebooks
# ===========================
.ipynb_checkpoints/

# ===========================
# ML / Data Science
# ===========================
*.h5
*.hdf5
*.pkl
*.pickle
*.joblib
*.tflite
*.onnx
*.pb
*.pt
*.pth
wandb/
mlruns/
*.tfevents*

# Trained model artifacts we do want in git
!ml-models/bloom_prediction/models/vanda_bloom_model.h5
!ml-models/bloom_prediction/models/tabular_scaler.pkl
!ml-models/growth_stage/models/vanda_growth_model.h5
!ml-models/growth_stage/models/label_encoder.pkl

# Large datasets (store in cloud, not git)
ml-models/*/data/raw/
ml-models/*/data/raw.zip
ml-models/*/data/processed/
ml-models/*/data/images/
ml-models/*/data/split/
ml-models/*/data/split_augmented/
ml-models/*/data/pending/
datasets/

# Raw annotated photos for the object detector (~300MB) - regenerate the
# trainable dataset from these with prepare_dataset.py
ml-models/shared/object_detection/data/flowers/
ml-models/shared/object_detection/data/plants/
ml-models/shared/object_detection/data/seeds/
ml-models/shared/object_detection/data/buds/

# Generated YOLO dataset (regenerate with prepare_dataset.py) and training runs
ml-models/shared/object_detection/data/yolo/
ml-models/shared/object_detection/runs/

# Colab upload/download bundles - regenerate with prepare_for_colab.py /
# zip_for_colab.py, or re-download from Drive; too large for git anyway
# (growth_stage/bloom_prediction colab zips exceed GitHub's 100MB file limit)
*_colab.zip
ml-models/trained models/

# ===========================
# React Native / Node.js
# ===========================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Metro bundler
.metro-health-check*

# ===========================
# Android
# ===========================
mobile/android/app/build/
mobile/android/build/
mobile/android/.gradle/
local.properties
*.apk
*.aab

# ===========================
# iOS
# ===========================
mobile/ios/Pods/
mobile/ios/build/
*.xcworkspace
*.xcuserstate
DerivedData/

# ===========================
# Firebase
# ===========================
.firebase/
firebase-debug.log
firebaserc

# ===========================
# Environment & Secrets
# ===========================
.env
.env.local
.env.production
.env.staging
*.pem
secrets/
config/local.py

# ===========================
# IDE & Editor
# ===========================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ===========================
# IoT / Embedded
# ===========================
iot/esp32/.pio/
iot/esp32/.vscode/

# ===========================
# Component 4 - Hybrid Pollination
# ===========================
# Embedding caches: regenerated by the training scripts, and useless to a
# clone anyway because data/images/ is not tracked. ~20 MB of dead weight.
ml-models/*/data/*_embeddings.npy
ml-models/*/data/*_embeddings.npz

# Spot-check previews for the auto-labelled flower set (regenerable)
ml-models/hybrid_pollination/results/flower_label_review/

# The DEPLOYED models are tracked despite the blanket *.pkl rule above.
# Without them the backend cannot start, and they cannot be rebuilt from a
# clone because the training photographs are not in the repository.
!ml-models/hybrid_pollination/models/best_model.pkl
!ml-models/hybrid_pollination/models/preprocessors.pkl
!ml-models/hybrid_pollination/models/trait_leaf_condition.pkl
!ml-models/hybrid_pollination/models/trait_plant_strength.pkl
!ml-models/hybrid_pollination/models/orchid_gate.pkl
!ml-models/hybrid_pollination/models/flower_filter.pkl

# Firebase service-account keys. These grant server-side access to the whole
# project - unlike google-services.json, which is only client config. The real
# one lives OUTSIDE this repo at ~/.orchid-secrets/firebase-admin.json; this
# pattern only exists so a stray copy cannot be committed by accident.
*firebase-adminsdk*.json
firebase-admin.json
serviceAccountKey.json

# The API key. The repo is PUBLIC - never commit the real one.
mobile/src/config/secret.js

# ===========================
# Component 1 - disease detection (R26-SE-018)
# ===========================

# Colab result downloads. Regenerable; orchid_colab.zip is 133 MB and GitHub
# refuses any file over 100 MB.
ml-models/*/orchid_results.zip
ml-models/*/orchid_final.zip

# Timestamped safety copies written by make_labels_template.py and
# label_severity.py. The real severity_labels.csv IS tracked; these are backups
# that would otherwise accumulate forever.
ml-models/*/data/severity_labels.before_labelling_*.csv
ml-models/*/data/severity_labels.backup_*.csv

# Written by update_manifest_severity.py before it rewrites a manifest.
*.csv.bak

# Temporary file compare_models.py writes to measure model size on disk.
ml-models/*/models/_size_probe.keras

# Scratch folders from the one-image augmentation demo in HOW_TO_RUN.md.
ml-models/*/tools/demo_in/
ml-models/*/tools/demo_out/

# Segmentation debug previews (regenerate with: python src/segmentation.py)
ml-models/hybrid_pollination/results/segmentation_check/

# 

# Subagent-driven-development scratch: ledgers, briefs, review packages.
# Git history is the durable record; this is the controller working memory.
.superpowers/
